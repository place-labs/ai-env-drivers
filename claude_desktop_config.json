{
  "mcpServers": {
    "markitdown": {
      "command": "docker",
      "args": [
        "run",
        "--rm",
        "-i",
        "-v",
        "./docs:/docs",
        "markitdown-mcp:latest"
      ]
    }
  }
}
